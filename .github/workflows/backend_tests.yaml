name: Backend Tests

on:
  push:
    branches:
      - release   # Работает только при пуше в ветку release
    paths:
      - 'src/backend/**'  # Работает только при изменении файлов в папке src


jobs:
  build_and_test:
    name: Build Docker image and run tests
    runs-on: self-hosted  # Используем self-hosted runner

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Клонируем репозиторий

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2  # Настроим Buildx для многоплатформенных сборок

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image using Dockerfile.backend-test
      run: |
        # Строим Docker-образ с использованием Dockerfile.backend-test
        docker build -f src/backend/Dockerfile.backend-test -t calculator-back:test ./src/backend

    - name: Run Docker container and execute tests
      run: |
        # Запускаем контейнер и тесты в фоновом режиме
        docker run --name calculator-back-test-container calculator-back:test
        # Docker автоматически выполнит тесты, так как CMD уже настроен в Dockerfile

    - name: Stop and remove Docker container
      run: |
        # Останавливаем контейнер после выполнения тестов
        docker stop calculator-back-test-container
        docker rm calculator-back-test-container

    - name: Remove Docker image
      run: |
        # Удаляем Docker-образ, чтобы освободить место
        docker rmi calculator-back:test