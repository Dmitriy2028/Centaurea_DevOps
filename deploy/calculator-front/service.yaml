apiVersion: v1
kind: Service
metadata:
  name: calculator-front-service
  namespace: app-front
spec:
  selector:
    app: calculator-front
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 80
  type: LoadBalancer