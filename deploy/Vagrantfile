# Automatically Generated by Vagrant Config Generator, see https://github.com/jianan1104/vagrantfile-generator
Vagrant.configure('2') do |config|
  (1..1).each do |i|
  config.vm.define "cluster" do |machine|
    machine.vm.box = 'ubuntu/focal64'
    machine.vm.network "public_network", ip: "192.168.100.101"
    machine.vm.hostname = "cluster"
    
    # Перенаправление портов
    machine.vm.network "forwarded_port", guest: 8081, host: 80, auto_correct: true
    machine.vm.network "forwarded_port", guest: 8081, host: 443, auto_correct: true

    machine.vm.provider "virtualbox" do |vb|
      vb.name = "k3d_cluster-#{i}"
      vb.cpus = '4'
      vb.memory = '8192'
      # vb.cpus = '2'
      # vb.memory = '4096'
    end
  end
  end
end
